import {RawPublication} from "queue";
import {LunchOfferEvent} from "./LunchOfferEvent";


export class LunchOfferEventFactory {

    public create(publication: RawPublication): LunchOfferEvent[] {
        return undefined;
    }
}
